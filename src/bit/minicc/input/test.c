
/* this is our test file*/

int main(){
	int i;
        
        //Deadline Next Tuesday (22
        //first, d is not declare (find variables use but not declare?)
        //a, b and i are use but not initialized (find variables declared but not used)
        //tree has been declare but not used (find variables used but not initialized)
        
        //build a report which variable that not declare
        //which variable should be initialized
        
        //Finish Final Exam solve this problem: a = b * 4 / 2 + c
        
	return d+4+2+1;
}

int foo(){
    foo1();
    return 0;
}

int foo1(){
    return 1;
}

